# MediCode 端到端运行记录

## 运行元数据

- **运行日期**：2026-08-28
- **仓库**：`C:\Users\Donghe\Desktop\04_MediCode码医\MediCode`
- **代码基线**：`b35908f`（工作区含本次改动及既有用户修改）
- **运行方式**：目标仓库后端，隔离 SQLite 数据库，HTTP API
- **LLM**：未启用；本次使用规则引擎和本地 ICD 索引
- **输入**：合成心内科出院病历，不含真实患者身份信息

## 启动与播种

服务启动后完成数据库建表、参考数据播种和演示数据播种。启动后的数据库计数为：

| 数据集 | 数量 |
|---|---:|
| `icd_codes` | 1,531 |
| `drg_groups` | 73 |
| `qc_rules` | 27 |
| `patients` | 500 |
| `medical_records` | 500 |
| `coding_results` | 500 |
| `qc_results` | 345 |

这次运行同时验证了独立参考数据初始化逻辑；缺失某一个参考数据集时，不会再被其他数据集的非空状态阻止。

## API 链路

以下接口均返回 HTTP 200，流水线保存接口返回 HTTP 200：

1. `POST /api/v1/auth/login`
2. `POST /api/v1/coding/auto-code`
3. `POST /api/v1/qc/check`
4. `POST /api/v1/drg/group`
5. `POST /api/v1/rejection/assess`
6. `POST /api/v1/pipeline/save`

## 结果摘要

| 阶段 | 实际结果 |
|---|---|
| 编码 | 主诊断 `I21.900`；9 个次要诊断；1 个手术候选；1,716 ms |
| 质控 | 95.0 分；1 个问题 |
| DRG | `FR11`；权重 2.52；预估支付 `30,240` |
| 拒付风险 | 90/100；高风险；可规避金额 `13,200` |
| 保存 | `record_id=501`；编码结果和 1 条 QC 结果均成功保存 |

## 结论与限制

- 规则模式下的“病历 → 编码 → 质控 → DRG → 拒付风险 → 保存”链路已在隔离数据库中跑通。
- 本记录证明的是当前环境下的可执行性，不证明临床准确率、医院部署效果或经济收益。
- LLM 增强路径未在本次运行中验证；编码结果仍必须由人工审核确认。
- 基准测试的准确性结果另见 `docs/BENCHMARK_REPORT.md`，当前仅有 4 例验证性测试。
