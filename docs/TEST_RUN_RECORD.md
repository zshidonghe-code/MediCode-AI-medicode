# MediCode 测试执行记录

## 执行元数据

- **执行日期**：2026-08-29
- **仓库**：`C:\Users\Donghe\Desktop\04_MediCode码医\MediCode`
- **代码版本**：`de39fc2`
- **测试命令**：`backend/.venv/Scripts/python.exe -m pytest -q --cov=src --cov-report=term-missing`
- **服务依赖**：启动本地 SQLite 数据库服务于 `127.0.0.1:8001`
- **LLM**：未启用，测试覆盖规则模式和接口降级路径

## 结果

| 指标 | 结果 |
|---|---:|
| 收集测试 | 86 |
| 通过 | 86 |
| 失败 | 0 |
| 错误 | 0 |
| 通过率 | 100% |
| 覆盖率 | 47%（3013 条语句，1589 条未覆盖） |

全量测试在最终源码版本上执行完成，耗时 46.77 秒；输出包含 2 个第三方依赖弃用警告。覆盖率由 `pytest-cov` 实际采集，不能据此推导临床准确率或部署效果。

## 单独回归范围

- 独立参考数据播种：ICD 非空、DRG/QC 为空时补齐缺失数据。
- 重复启动保护：三个参考数据集均已存在时不执行播种；生命周期连续启动两次时演示数据只初始化一次。
- API 集成测试：认证、编码、质控、DRG、流水线、拒付风险和管理接口。

## 质量检查

| 工具 | 命令 | 结果 |
|---|---|---|
| Ruff（本次改动相关文件） | `ruff check src/main.py src/scripts/seed_data.py tests/test_seed_data.py tests/test_startup.py` | 通过 |
| Ruff（全部源码） | `ruff check src` | 未通过：200 项既有问题，91 项可自动修复 |
| mypy（全部源码） | `mypy src` | 未通过：14 个文件共 31 项类型问题 |

全库静态检查结果作为后续代码质量工作项保留；本次修正了启动与播种模块附近的导入和类型问题，没有对无关模块进行批量重构。

## 限制

- 该记录证明当前代码在本机本地 SQLite 数据库和规则模式下通过测试，并记录了代码覆盖率；不证明临床准确率、医院部署效果或 LLM 增益。
- 当前覆盖率为测试覆盖率指标，不是临床准确率指标；Ruff/mypy 全库问题仍需后续专项治理。
