# MediCode 测试执行记录

## 执行元数据

- **执行日期**：2026-08-29
- **仓库**：`C:\Users\Donghe\Desktop\04_MediCode码医\MediCode`
- **代码版本**：`master` 静态治理提交后
- **测试命令**：`backend/.venv/Scripts/python.exe -m pytest tests -q`
- **服务依赖**：启动本地 SQLite 数据库服务于 `127.0.0.1:8000`
- **LLM**：未启用，测试覆盖规则模式和接口降级路径

## 结果

| 指标 | 结果 |
|---|---:|
| 收集测试 | 87 |
| 通过 | 87 |
| 失败 | 0 |
| 错误 | 0 |
| 通过率 | 100% |
| 覆盖率 | 47%（上一轮完整覆盖率采集；本轮未重复采集） |

全量测试在最终源码和依赖版本上执行完成，耗时 58.48 秒。覆盖率由上一轮 `pytest-cov` 实际采集，不能据此推导临床准确率或部署效果。

## 单独回归范围

- 独立参考数据播种：ICD 非空、DRG/QC 为空时补齐缺失数据。
- 重复启动保护：三个参考数据集均已存在时不执行播种；生命周期连续启动两次时演示数据只初始化一次。
- API 集成测试：认证、编码、质控、DRG、流水线、拒付风险和管理接口。
- 向量检索最小运行验证：`scikit-learn 1.9.0` 构建 TF-IDF 索引并返回预期 ICD 编码。
- LLM DRG 回退：Ollama 请求异常时，明确转交规则后端生成建议。

## 质量检查

| 工具 | 命令 | 结果 |
|---|---|---|
| Ruff（全部源码） | `ruff check src` | 通过 |
| Ruff 格式 | `ruff format --check src` | 54 个文件已格式化 |
| mypy（全部源码） | `mypy src` | 通过：54 个源文件 |

Ruff 对 FastAPI 依赖声明（B008）仅在 API endpoint 文件中豁免；`str + Enum`（UP042）保留以维持现有数据库枚举序列化语义。其余启用的 Ruff 规则均已通过。

## 限制

- 该记录证明当前代码在本机本地 SQLite 数据库和规则模式下通过测试，并记录了代码覆盖率；不证明临床准确率、医院部署效果或 LLM 增益。
- 当前覆盖率为测试覆盖率指标，不是临床准确率指标；通过静态检查不等同于临床准确率或真实医院部署验收。

---

## 复测记录：2026-09-06

### 执行元数据

- **执行日期**：2026-09-06
- **代码版本**：`master`，编码修复 commit `d09852c`（手术候选爆炸三层修复）
- **测试命令**：先 `uvicorn src.main:app` 启动服务，再 `pytest tests -q`
- **⚠️ 重要前提**：后端测试通过 httpx 访问真实 `localhost:8000`，**必须先启动 uvicorn 服务**，否则 8 failed + 36 errors 全部为 ConnectError（此为本轮实测确认的隐藏前提，已写入开发注意事项）

### 结果

| 指标 | 结果 |
|---|---:|
| 通过 | 91 |
| 失败 | 0 |
| 错误 | 0 |
| 通过率 | 100% |

测试数从 87 → 91 的差异为后续开发新增的测试用例。

### 覆盖范围

- API 集成测试：认证、编码、质控、DRG、流水线、拒付风险、病历复审 Agent、管理、仪表盘接口。
- 编码基准（`tests/benchmark_accuracy.py`）：4/4 全部通过（含 2026-09-06 修复的手术编码抽取与否定词「无」两例）。
- **新增：注入式质控基准（`tests/benchmark_qc.py` + `tests/fixtures/competition_cases.json`）**：400 份合成病历（340 份注入已知缺陷 + 60 份干净对照），固定随机种子 20260906 可复现。结果：17 条质控规则缺陷召回 100%（17/17），误报率 0%（此前为 21.7%，经手术候选爆炸修复后归零）。详细数据见 `docs/QC_BENCHMARK_REPORT.md`。

### 质量检查

Ruff check / Ruff format / mypy 全部通过。

### 与上一轮（2026-08-29）的主要代码变化

- 编码引擎：手术实体噪声过滤、诊断实体检查前缀清洗、向量搜索兜底 0.55 分数下限、同亚目唯一去重、未特指损伤码惩罚。
- 质控引擎：QC-201 死规则修复（贪婪正则导致规则从未触发）、手术-诊断一致性检查按手术族聚合章节。
- NLP 引擎：支架类手术词表、否定词补「无」、否定窗口按分句限定。
- 修复 `_local_search` UnboundLocalError 崩溃（注入式基准首跑即发现）。
