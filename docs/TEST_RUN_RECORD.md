# MediCode 测试执行记录

## 执行元数据

- **执行日期**：2026-08-29
- **仓库**：`C:\Users\Donghe\Desktop\04_MediCode码医\MediCode`
- **代码版本**：`master` 静态治理提交后
- **测试命令**：`backend/.venv/Scripts/python.exe -m pytest tests -q`
- **服务依赖**：启动本地 SQLite 数据库服务于 `127.0.0.1:8000`
- **LLM**：未启用，测试覆盖规则模式和接口降级路径

## 结果

| 指标 | 结果 |
|---|---:|
| 收集测试 | 87 |
| 通过 | 87 |
| 失败 | 0 |
| 错误 | 0 |
| 通过率 | 100% |
| 覆盖率 | 47%（上一轮完整覆盖率采集；本轮未重复采集） |

全量测试在最终源码和依赖版本上执行完成，耗时 58.48 秒。覆盖率由上一轮 `pytest-cov` 实际采集，不能据此推导临床准确率或部署效果。

## 单独回归范围

- 独立参考数据播种：ICD 非空、DRG/QC 为空时补齐缺失数据。
- 重复启动保护：三个参考数据集均已存在时不执行播种；生命周期连续启动两次时演示数据只初始化一次。
- API 集成测试：认证、编码、质控、DRG、流水线、拒付风险和管理接口。
- 向量检索最小运行验证：`scikit-learn 1.9.0` 构建 TF-IDF 索引并返回预期 ICD 编码。
- LLM DRG 回退：Ollama 请求异常时，明确转交规则后端生成建议。

## 质量检查

| 工具 | 命令 | 结果 |
|---|---|---|
| Ruff（全部源码） | `ruff check src` | 通过 |
| Ruff 格式 | `ruff format --check src` | 54 个文件已格式化 |
| mypy（全部源码） | `mypy src` | 通过：54 个源文件 |

Ruff 对 FastAPI 依赖声明（B008）仅在 API endpoint 文件中豁免；`str + Enum`（UP042）保留以维持现有数据库枚举序列化语义。其余启用的 Ruff 规则均已通过。

## 限制

- 该记录证明当前代码在本机本地 SQLite 数据库和规则模式下通过测试，并记录了代码覆盖率；不证明临床准确率、医院部署效果或 LLM 增益。
- 当前覆盖率为测试覆盖率指标，不是临床准确率指标；通过静态检查不等同于临床准确率或真实医院部署验收。
