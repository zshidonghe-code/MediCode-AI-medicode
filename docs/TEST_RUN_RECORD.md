# MediCode 测试执行记录

## 执行元数据

- **执行日期**：2026-08-29
- **仓库**：`C:\Users\Donghe\Desktop\04_MediCode码医\MediCode`
- **代码版本**：`664cd88` 及本次回归测试补充
- **测试命令**：`backend/.venv/Scripts/python.exe -m pytest -q`
- **服务依赖**：启动隔离 SQLite 数据库服务于 `127.0.0.1:8001`
- **LLM**：未启用，测试覆盖规则模式和接口降级路径

## 结果

| 指标 | 结果 |
|---|---:|
| 收集测试 | 84 |
| 通过 | 84 |
| 失败 | 0 |
| 错误 | 0 |
| 通过率 | 100% |
| 覆盖率 | 未采集 |

全量测试由提交前钩子实际执行完成，耗时 41.55 秒；输出包含 2 个第三方依赖弃用警告。当前虚拟环境未安装 `pytest-cov` 或 `coverage`，因此没有生成代码覆盖率百分比，不能将覆盖率写成估算值。

## 单独回归范围

- 独立参考数据播种：ICD 非空、DRG/QC 为空时补齐缺失数据。
- 重复启动保护：三个参考数据集均已存在时不执行播种。
- API 集成测试：认证、编码、质控、DRG、流水线、拒付风险和管理接口。

## 限制

- 该记录证明当前代码在本机隔离数据库和规则模式下通过测试，不证明临床准确率、医院部署效果或 LLM 增益。
- 覆盖率需在安装 `pytest-cov` 后重新执行并补充到本记录。
